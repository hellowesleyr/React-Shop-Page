import React from "react";

const Checkout = ({ cart }) => {
return (

    <div className="checkout">
        <div className="cartPanel">
            <h1>Checkout</h1>
        </div>
    </div>
)
}

export default Checkout;